# Taller: creación de navegación interna con anclas

**Recordatorio teórico** Las anclas HTML permiten crear enlaces de navegación interna dentro de una misma página. El mecanismo se basa en dos elementos complementarios: el punto de llegada (destino) y el enlace que apunta hacia él. El destino se define añadiendo el atributo `id` a cualquier etiqueta HTML existente, por ejemplo ` ``` Esta técnica es especialmente útil en páginas largas como un currículum, un portafolio o una documentación técnica. --- **Enunciado del taller** En este taller vas a enriquecer el currículum semántico que creaste en el miniproyecto anterior. Hasta ahora tu página tiene estructura, etiquetas semánticas, títulos jerárquicos y una lista de habilidades. Sin embargo, si la página es larga, el usuario debe desplazarse manualmente para encontrar cada sección. Tu objetivo en este taller es añadir un sistema de navegación interna que permita al visitante saltar directamente a cualquier sección del currículum con un solo clic. Comienza por revisar tu archivo HTML existente e identifica todas las secciones principales del currículum: por ejemplo, «Sobre mí», «Experiencia», «Formación», «Habilidades» y «Contacto». Cada una de estas secciones debe convertirse en un destino de ancla. Para ello, asegúrate de que cada sección o su título principal lleva un atributo `id` significativo, único y bien formado. A continuación, crea una barra de navegación interna utilizando la etiqueta semántica apropiada (``) y dentro de ella una lista (``) cuyos elementos (``) contengan cada uno un enlace (``) que apunte al `id` correspondiente mediante el símbolo `#`. Esta barra de navegación debe colocarse en la...

[Ver el taller completo](https://www.ilaria-academy.com/nextjob/full-stack-web-developer/es/27ff0726-3de4-4616-b0be-d9a166415d04/7bcd8b98-22da-4bf0-892c-53debc0a2ed0/taller-creacion-de-navegacion-interna-con-anclas)
